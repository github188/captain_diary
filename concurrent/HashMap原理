http://www.importnew.com/10620.html

简单抽象总结一下，HashMap 有两层存储，第一层是个16个长度的Entry数组，存储不同key的hash值value，如果hash值相同，则将相同hash值的value以
链表方式存储。
这里面提到了  HashMap$Entry 这个东西表示什么，表示类HashMap下面有个Entry，他是前者的内部类。

jdk1.8里面已经在这里更新了，这个内部类不叫Entry了，而是叫做Node，但还是实现自Map.Entry这个接口




