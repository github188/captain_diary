以下文字，绝对没有粘贴，完全是口述完成，就像高考考场一样。
--------整备一下高考试卷一下先-----------
HashMap实现自Map接口，按照键值对KV的结构来存储，首先说他是有几层结构的。
往里看，第一层结构是一个Table数组，这个数组的大小是可变的，构造器里面没有指定这个数组长度的情况下，默认是容纳16个元素的数组。默认的加载因子为0.75，
也就是说，当存储的数组元素个数超过16*0.75也就是12这个数字之后，就会调用resize（）方法，将数组容量加倍。
我们可以将数组中的每一个元素称之为一个桶，那么这个桶里面存的是链表或者二叉树，jdk8之前只能存链表一种数据结构，从8开始可以存储链表和二叉树，如果每一个
桶里面的存储的元素个数超过一定数量，就会以二叉树的形式来存储。
再来说，HashMap里面是如何来存储数据的，从HashMap这个名字上也看得出来，她是一种Hash类型的集合，他是根据key的哈希值，根据之歌哈希值对桶的数量取余数
，来决定该元素存到哪一个桶里面去。
所以，每个桶里面存放的一定是key的哈希值相同的键值对。存进来的时候，发现key的哈希值已经在在桶当中存在，怎么办呢，再用equals方法去比较地址值，如果相同
表示该key已经存在，所以会用新对象覆盖key值相同的老对象。 因为使用了Hash值作比较，所以在存放的时候，判断是否存在的算法在时间复杂度上更低了。

取数据的时候，根据先根据key计算哈希值，来判断放在哪一个桶里面，然后再去链表或者二叉树里面去查找，这样就大大提高效率了。   比不使用Hash值得Map来讲
效率高了很多。


















